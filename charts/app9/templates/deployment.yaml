apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app9.fullname" . }}
  labels:
    {{- include "app9.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "app9.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app9.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.app9.image.repository }}:{{ .Values.app9.image.tag }}"
          imagePullPolicy: {{ .Values.app9.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app10.fullname" . }}
  labels:
    {{- include "app10.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "app10.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app10.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.app10.image.repository }}:{{ .Values.app10.image.tag }}"
          imagePullPolicy: {{ .Values.app10.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}

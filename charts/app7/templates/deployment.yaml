apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app7.fullname" . }}
  labels:
    {{- include "app7.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "app7.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app7.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.app7.image.repository }}:{{ .Values.app7.image.tag }}"
          imagePullPolicy: {{ .Values.app7.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}

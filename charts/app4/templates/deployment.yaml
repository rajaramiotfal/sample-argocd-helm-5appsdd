apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app4.fullname" . }}
  labels:
    {{- include "app4.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "app4.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app4.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.app4.image.repository }}:{{ .Values.app4.image.tag }}"
          imagePullPolicy: {{ .Values.app4.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}

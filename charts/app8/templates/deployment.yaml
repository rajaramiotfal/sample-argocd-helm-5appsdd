apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app8.fullname" . }}
  labels:
    {{- include "app8.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "app8.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app8.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.app8.image.repository }}:{{ .Values.app8.image.tag }}"
          imagePullPolicy: {{ .Values.app8.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}

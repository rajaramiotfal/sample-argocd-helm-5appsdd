apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app5.fullname" . }}
  labels:
    {{- include "app5.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "app5.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app5.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.app5.image.repository }}:{{ .Values.app5.image.tag }}"
          imagePullPolicy: {{ .Values.app5.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.containerPort }}
